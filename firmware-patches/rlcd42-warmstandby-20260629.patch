diff --git a/main/CMakeLists.txt b/main/CMakeLists.txt
index 170cb18..9f90862 100644
--- a/main/CMakeLists.txt
+++ b/main/CMakeLists.txt
@@ -452,6 +452,14 @@ elseif(CONFIG_BOARD_TYPE_WAVESHARE_ESP32_S3_RLCD_4_2)
     set(MANUFACTURER "waveshare")
     set(BOARD_TYPE "esp32-s3-rlcd-4.2")
     set(BUILTIN_TEXT_FONT font_puhui_basic_30_4)
+    set(ASSETS_TEXT_FONT font_puhui_basic_30_4)   # 张老师盒子：要 assets 打 common 全字集，必须传 **basic** 名——脚本 get_text_font_path 会把 basic→common.bin（cbin/font_puhui_common_30_4.bin, 2.5MB）打进 assets。传 common 名会被判"没 basic"→返回 None→不打字体→缺字（踩过的坑）
+    # 张老师盒子：再把 16px common 全字集(843KB)打进 assets —— 小字(上句/本节课/状态)用它，否则落 app 的 basic 子集缺字。
+    # --extra_files 收"目录"，故放一个只含该字体的暂存目录（别把 cbin 里所有字体都打进来）；运行时 custom_lcd_display 按名 GetAssetData 取用。
+    set(_box_extra_dir "${CMAKE_BINARY_DIR}/box_extra_assets")
+    file(MAKE_DIRECTORY "${_box_extra_dir}")
+    file(COPY "${CMAKE_CURRENT_LIST_DIR}/../managed_components/78__xiaozhi-fonts/cbin/font_puhui_common_16_4.bin"
+         DESTINATION "${_box_extra_dir}")
+    set(DEFAULT_ASSETS_EXTRA_FILES "${_box_extra_dir}")
     set(BUILTIN_ICON_FONT font_awesome_30_4)
 elseif(CONFIG_BOARD_TYPE_WAVESHARE_ESP32_S3_TOUCH_LCD_3_49)
     set(MANUFACTURER "waveshare")
@@ -1145,8 +1153,12 @@ function(build_default_assets_bin)
         "--output" "${GENERATED_ASSETS_BIN}"
     )
 
-    # Add builtin text font if defined
-    if(BUILTIN_TEXT_FONT)
+    # Add text font for assets. Prefer ASSETS_TEXT_FONT (a fuller font that lives only in the
+    # assets partition) over the app-embedded BUILTIN_TEXT_FONT, so on-screen text isn't limited
+    # to the basic subset.
+    if(ASSETS_TEXT_FONT)
+        list(APPEND BUILD_ARGS "--builtin_text_font" "${ASSETS_TEXT_FONT}")
+    elseif(BUILTIN_TEXT_FONT)
         list(APPEND BUILD_ARGS "--builtin_text_font" "${BUILTIN_TEXT_FONT}")
     endif()
 
diff --git a/main/application.cc b/main/application.cc
index d54ac6b..eff237c 100644
--- a/main/application.cc
+++ b/main/application.cc
@@ -576,6 +576,15 @@ void Application::InitializeProtocol() {
                     Schedule([this]() {
                         Reboot();
                     });
+                } else if (strcmp(command->valuestring, "standby") == 0) {
+                    // 暖待命：进 Idle(关上行麦/开唤醒词/显待命屏)，但保持 WS 不断，
+                    // 便于服务器在提醒到点时推 alert 过来。唤醒词/按键会在原连接上恢复听。
+                    Schedule([this]() {
+                        auto s = GetDeviceState();
+                        if (s == kDeviceStateListening || s == kDeviceStateSpeaking) {
+                            SetDeviceState(kDeviceStateIdle);
+                        }
+                    });
                 } else {
                     ESP_LOGW(TAG, "Unknown system command: %s", command->valuestring);
                 }
@@ -585,7 +594,9 @@ void Application::InitializeProtocol() {
             auto message = cJSON_GetObjectItem(root, "message");
             auto emotion = cJSON_GetObjectItem(root, "emotion");
             if (cJSON_IsString(status) && cJSON_IsString(message) && cJSON_IsString(emotion)) {
-                Alert(status->valuestring, message->valuestring, emotion->valuestring, Lang::Sounds::OGG_VIBRATION);
+                Alert(status->valuestring, message->valuestring, emotion->valuestring, Lang::Sounds::OGG_POPUP);
+                // 把提醒文字弹到屏上(暖待命时钟屏之上也可见)，~10s 自动隐
+                Board::GetInstance().GetDisplay()->ShowNotification(message->valuestring, 10000);
             } else {
                 ESP_LOGW(TAG, "Alert command requires status, message and emotion");
             }
diff --git a/main/boards/waveshare/esp32-s3-rlcd-4.2/config.h b/main/boards/waveshare/esp32-s3-rlcd-4.2/config.h
index 47b6c71..d3cb309 100644
--- a/main/boards/waveshare/esp32-s3-rlcd-4.2/config.h
+++ b/main/boards/waveshare/esp32-s3-rlcd-4.2/config.h
@@ -24,6 +24,7 @@
 #define AUDIO_CODEC_ES7210_ADDR  ES7210_CODEC_DEFAULT_ADDR
 
 #define BOOT_BUTTON_GPIO        GPIO_NUM_0
+#define KEY_BUTTON_GPIO         GPIO_NUM_18   // 侧键 KEY：单击=暂停/恢复、长按=音量+一档（GPIO 待真机校；不灵换 GPIO 重试）
 
 
 #define RLCD_DC_PIN    GPIO_NUM_5  
diff --git a/main/boards/waveshare/esp32-s3-rlcd-4.2/custom_lcd_display.cc b/main/boards/waveshare/esp32-s3-rlcd-4.2/custom_lcd_display.cc
index ba5d81d..53bf143 100644
--- a/main/boards/waveshare/esp32-s3-rlcd-4.2/custom_lcd_display.cc
+++ b/main/boards/waveshare/esp32-s3-rlcd-4.2/custom_lcd_display.cc
@@ -1,5 +1,7 @@
 #include <vector>
 #include <cstring>
+#include <cstdio>
+#include <ctime>
 #include <freertos/FreeRTOS.h>
 #include <esp_lcd_panel_io.h>
 #include <esp_log.h>
@@ -11,6 +13,14 @@
 #include "settings.h"
 #include "config.h"
 #include "board.h"
+#include "application.h"   // Application::GetInstance().GetDeviceState()：判 idle
+#include "device_state.h"  // kDeviceStateIdle
+#include "lvgl_theme.h"
+#include "assets.h"        // 取 assets 里打进去的 16px common 全字集
+#include <cbin_font.h>     // LvglCBinFont：把 assets 的 .bin 字体包成 lv_font_t
+#include <font_awesome.h>  // FONT_AWESOME_WIFI* ：用 GetNetworkStateIcon 结果映射信号格数
+
+LV_FONT_DECLARE(font_puhui_basic_16_4);   // 小号(16px)：顶条/底部/上句；当前句用主题 30px
 
 void CustomLcdDisplay::Lvgl_flush_cb(lv_display_t * disp, const lv_area_t * area, uint8_t * color_p)
 {
@@ -40,14 +50,16 @@ bool mirror_x,
 bool mirror_y, 
 bool swap_xy,
 spi_display_config_t spiconfig,
-spi_host_device_t spi_host) : LcdDisplay(panel_io, panel, width, height),
+spi_host_device_t spi_host,
+i2c_master_bus_handle_t i2c_bus) : LcdDisplay(panel_io, panel, width, height),
 mosi_(spiconfig.mosi),
-scl_(spiconfig.scl), 
-dc_(spiconfig.dc), 
-cs_(spiconfig.cs), 
-rst_(spiconfig.rst), 
-width_(width), 
-height_(height)
+scl_(spiconfig.scl),
+dc_(spiconfig.dc),
+cs_(spiconfig.cs),
+rst_(spiconfig.rst),
+width_(width),
+height_(height),
+i2c_bus_(i2c_bus)
 {
 	ESP_LOGI(TAG, "Initialize SPI");
 	esp_err_t        ret;
@@ -336,3 +348,741 @@ void CustomLcdDisplay::RLCD_Display() {
 
 	RLCD_Sendbuffera(DispBuffer,DisplayLen);
 }
+
+// ============ 张老师盒子·黑白屏重排 ============
+// 1-bit 反射屏：只黑白，层次靠大小/位置/反白；不用半透明灰（threshold 会糟）。
+void CustomLcdDisplay::SetupUI() {
+    if (setup_ui_called_) {
+        return;
+    }
+    Display::SetupUI();
+    DisplayLockGuard lock(this);
+
+    auto theme = static_cast<LvglTheme*>(current_theme_);
+    auto big_font = theme->text_font()->font();            // 30px：字幕当前句
+    auto icon_font = theme->icon_font()->font();           // 图标(wifi/电量/mute)
+    const lv_font_t* small_font = &font_puhui_basic_16_4;  // 16px：顶条/底部/上句
+    lv_color_t black = lv_color_black();
+    lv_color_t white = lv_color_white();
+
+    lv_obj_t* screen = lv_screen_active();
+    lv_obj_set_style_text_font(screen, small_font, 0);
+    lv_obj_set_style_text_color(screen, black, 0);
+    lv_obj_set_style_bg_color(screen, white, 0);
+
+    /* 背景容器：全屏白底（仅背景，不做布局，避免挤压子项） */
+    container_ = lv_obj_create(screen);
+    lv_obj_set_size(container_, LV_HOR_RES, LV_VER_RES);
+    lv_obj_set_style_radius(container_, 0, 0);
+    lv_obj_set_style_pad_all(container_, 0, 0);
+    lv_obj_set_style_border_width(container_, 0, 0);
+    lv_obj_set_style_bg_color(container_, white, 0);
+    lv_obj_remove_flag(container_, LV_OBJ_FLAG_SCROLLABLE);
+
+    /* ---- 顶条：贴顶。左 wifi(flex) | 右 用户·学科+mute+电量(flex) | 中 状态(浮动·真居中) ---- */
+    top_bar_ = lv_obj_create(screen);
+    lv_obj_set_width(top_bar_, LV_HOR_RES);
+    lv_obj_set_height(top_bar_, LV_SIZE_CONTENT);
+    lv_obj_set_style_radius(top_bar_, 0, 0);
+    lv_obj_set_style_pad_top(top_bar_, 5, 0);
+    lv_obj_set_style_pad_bottom(top_bar_, 5, 0);
+    lv_obj_set_style_pad_left(top_bar_, 8, 0);
+    lv_obj_set_style_pad_right(top_bar_, 8, 0);
+    lv_obj_set_style_bg_color(top_bar_, white, 0);
+    lv_obj_set_style_border_color(top_bar_, black, 0);
+    lv_obj_set_style_border_width(top_bar_, 1, 0);
+    lv_obj_set_style_border_side(top_bar_, LV_BORDER_SIDE_BOTTOM, 0);
+    lv_obj_set_style_text_font(top_bar_, small_font, 0);
+    lv_obj_set_flex_flow(top_bar_, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(top_bar_, LV_FLEX_ALIGN_SPACE_BETWEEN, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(top_bar_, LV_OBJ_FLAG_SCROLLABLE);
+    lv_obj_align(top_bar_, LV_ALIGN_TOP_MID, 0, 0);
+
+    // 左：wifi。细弧字形在 1-bit 屏会被阈值吃成一个点 → 改实心信号格；network_label_ 保留但隐藏，
+    // 基类 UpdateStatusBar 仍往里写字形(不可见、无害)，免改基类。隐藏项不占 flex 布局。
+    network_label_ = lv_label_create(top_bar_);
+    lv_label_set_text(network_label_, "");
+    lv_obj_set_style_text_font(network_label_, icon_font, 0);
+    lv_obj_add_flag(network_label_, LV_OBJ_FLAG_HIDDEN);
+    MakeSignalBars(top_bar_, conv_wifi_bars_);     // 可见的实心信号格（flex 左端）
+
+    lv_obj_t* right = lv_obj_create(top_bar_);     // 右：用户·学科 + mute + 电量（flex 右端）
+    lv_obj_set_size(right, LV_SIZE_CONTENT, LV_SIZE_CONTENT);
+    lv_obj_set_style_bg_opa(right, LV_OPA_TRANSP, 0);
+    lv_obj_set_style_border_width(right, 0, 0);
+    lv_obj_set_style_pad_all(right, 0, 0);
+    lv_obj_set_flex_flow(right, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(right, LV_FLEX_ALIGN_END, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(right, LV_OBJ_FLAG_SCROLLABLE);
+    who_label_ = lv_label_create(right);
+    lv_label_set_text(who_label_, "");
+    lv_obj_set_style_text_font(who_label_, small_font, 0);
+    mute_label_ = lv_label_create(right);
+    lv_label_set_text(mute_label_, "");
+    lv_obj_set_style_text_font(mute_label_, icon_font, 0);
+    lv_obj_set_style_margin_left(mute_label_, 6, 0);
+    // 电量：同理改实心电池；battery_label_ 保留隐藏（基类照写字形 + 低电量弹窗逻辑仍用它）。
+    battery_label_ = lv_label_create(right);
+    lv_label_set_text(battery_label_, "");
+    lv_obj_set_style_text_font(battery_label_, icon_font, 0);
+    lv_obj_add_flag(battery_label_, LV_OBJ_FLAG_HIDDEN);
+    lv_obj_t* conv_bat_box = MakeSolidBattery(right, &conv_bat_fill_);  // 可见的实心电池（flex 右端）
+    lv_obj_set_style_margin_left(conv_bat_box, 6, 0);
+
+    // 中：状态文字——IGNORE_LAYOUT 不参与 flex、对齐屏幕中心 → 真·居中（不受左右宽度影响）
+    status_label_ = lv_label_create(top_bar_);
+    lv_obj_add_flag(status_label_, LV_OBJ_FLAG_IGNORE_LAYOUT);
+    lv_obj_set_width(status_label_, lv_pct(55));
+    lv_obj_set_style_text_align(status_label_, LV_TEXT_ALIGN_CENTER, 0);
+    lv_obj_set_style_text_font(status_label_, small_font, 0);
+    lv_label_set_text(status_label_, Lang::Strings::INITIALIZING);
+    lv_obj_align(status_label_, LV_ALIGN_CENTER, 0, 0);
+
+    notification_label_ = lv_label_create(top_bar_);
+    lv_obj_add_flag(notification_label_, LV_OBJ_FLAG_IGNORE_LAYOUT);
+    lv_obj_set_width(notification_label_, lv_pct(55));
+    lv_obj_set_style_text_align(notification_label_, LV_TEXT_ALIGN_CENTER, 0);
+    lv_obj_set_style_text_font(notification_label_, small_font, 0);
+    lv_label_set_text(notification_label_, "");
+    lv_obj_align(notification_label_, LV_ALIGN_CENTER, 0, 0);
+    lv_obj_add_flag(notification_label_, LV_OBJ_FLAG_HIDDEN);
+
+    /* ---- 底部：本节课主要内容。贴底·必显 ---- */
+    side_bar_ = lv_obj_create(screen);
+    lv_obj_set_width(side_bar_, LV_HOR_RES);
+    lv_obj_set_height(side_bar_, LV_SIZE_CONTENT);
+    lv_obj_set_style_radius(side_bar_, 0, 0);
+    lv_obj_set_style_pad_top(side_bar_, 5, 0);
+    lv_obj_set_style_pad_bottom(side_bar_, 5, 0);
+    lv_obj_set_style_pad_left(side_bar_, 10, 0);
+    lv_obj_set_style_pad_right(side_bar_, 10, 0);
+    lv_obj_set_style_bg_color(side_bar_, white, 0);
+    lv_obj_set_style_border_color(side_bar_, black, 0);
+    lv_obj_set_style_border_width(side_bar_, 1, 0);
+    lv_obj_set_style_border_side(side_bar_, LV_BORDER_SIDE_TOP, 0);
+    lv_obj_remove_flag(side_bar_, LV_OBJ_FLAG_SCROLLABLE);
+    lv_obj_align(side_bar_, LV_ALIGN_BOTTOM_MID, 0, 0);
+    topic_label_ = lv_label_create(side_bar_);
+    lv_label_set_text(topic_label_, "本节课：待开课");
+    lv_obj_set_style_text_font(topic_label_, small_font, 0);
+    lv_obj_set_width(topic_label_, lv_pct(100));
+    lv_label_set_long_mode(topic_label_, LV_LABEL_LONG_DOT);
+    lv_obj_set_style_text_align(topic_label_, LV_TEXT_ALIGN_CENTER, 0);
+
+    /* ---- 中央：字幕（上句小 / 当前句大），夹在顶/底之间、屏幕居中 ---- */
+    content_ = lv_obj_create(screen);
+    lv_obj_set_width(content_, LV_HOR_RES);
+    lv_obj_set_height(content_, LV_VER_RES - 80);
+    lv_obj_set_style_radius(content_, 0, 0);
+    lv_obj_set_style_border_width(content_, 0, 0);
+    lv_obj_set_style_bg_color(content_, white, 0);
+    lv_obj_set_style_pad_left(content_, 14, 0);
+    lv_obj_set_style_pad_right(content_, 14, 0);
+    lv_obj_set_style_pad_top(content_, 2, 0);
+    lv_obj_set_style_pad_bottom(content_, 2, 0);
+    lv_obj_set_flex_flow(content_, LV_FLEX_FLOW_COLUMN);
+    lv_obj_set_flex_align(content_, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(content_, LV_OBJ_FLAG_SCROLLABLE);
+    lv_obj_align(content_, LV_ALIGN_CENTER, 0, 0);
+
+    prev_label_ = lv_label_create(content_);
+    lv_label_set_text(prev_label_, "");
+    lv_obj_set_style_text_font(prev_label_, small_font, 0);
+    lv_obj_set_width(prev_label_, LV_HOR_RES - 28);
+    lv_label_set_long_mode(prev_label_, LV_LABEL_LONG_DOT);
+    lv_obj_set_style_text_align(prev_label_, LV_TEXT_ALIGN_CENTER, 0);
+
+    chat_message_label_ = lv_label_create(content_);
+    lv_label_set_text(chat_message_label_, "");
+    lv_obj_set_style_text_font(chat_message_label_, big_font, 0);
+    lv_obj_set_width(chat_message_label_, LV_HOR_RES - 28);
+    lv_label_set_long_mode(chat_message_label_, LV_LABEL_LONG_WRAP);
+    lv_obj_set_style_text_align(chat_message_label_, LV_TEXT_ALIGN_CENTER, 0);
+    lv_obj_set_style_pad_top(chat_message_label_, 4, 0);
+
+    /* ---- emoji（本版不显表情，但基类 SetTheme 在 1199 行无判空地引用 emoji_label_，必须建出来·隐藏，否则空指针崩） ---- */
+    emoji_image_ = lv_image_create(screen);
+    lv_obj_add_flag(emoji_image_, LV_OBJ_FLAG_HIDDEN);
+    emoji_label_ = lv_label_create(screen);
+    lv_label_set_text(emoji_label_, "");
+    lv_obj_set_style_text_color(emoji_label_, black, 0);
+    lv_obj_add_flag(emoji_label_, LV_OBJ_FLAG_HIDDEN);
+
+    /* ---- 待命屏：整屏白底时钟，盖在对话版面之上；idle 时由 timer 显示 ---- */
+    SetupIdleScreen(screen, big_font, small_font);
+
+    /* ---- 低电量弹窗（隐藏；基类 UpdateStatusBar 会用到。建在待命屏之后→z 序在最上，低电量能盖住时钟） ---- */
+    low_battery_popup_ = lv_obj_create(screen);
+    lv_obj_set_scrollbar_mode(low_battery_popup_, LV_SCROLLBAR_MODE_OFF);
+    lv_obj_set_size(low_battery_popup_, LV_HOR_RES * 0.9, LV_SIZE_CONTENT);
+    lv_obj_align(low_battery_popup_, LV_ALIGN_BOTTOM_MID, 0, -8);
+    lv_obj_set_style_bg_color(low_battery_popup_, black, 0);
+    lv_obj_set_style_radius(low_battery_popup_, 6, 0);
+    low_battery_label_ = lv_label_create(low_battery_popup_);
+    lv_label_set_text(low_battery_label_, Lang::Strings::BATTERY_NEED_CHARGE);
+    lv_obj_set_style_text_color(low_battery_label_, white, 0);
+    lv_obj_set_style_text_font(low_battery_label_, small_font, 0);
+    lv_obj_center(low_battery_label_);
+    lv_obj_add_flag(low_battery_popup_, LV_OBJ_FLAG_HIDDEN);
+
+    /* ---- 音量条：双击 KEY 调音量时弹出（建在最后→z 序最上，盖住时钟/对话）。
+       基类 ShowNotification 在本屏是空壳不绘制，故自画一个居中黑卡 + 5 格白块。---- */
+    vol_popup_ = lv_obj_create(screen);
+    lv_obj_set_scrollbar_mode(vol_popup_, LV_SCROLLBAR_MODE_OFF);
+    lv_obj_set_size(vol_popup_, LV_SIZE_CONTENT, LV_SIZE_CONTENT);
+    lv_obj_align(vol_popup_, LV_ALIGN_CENTER, 0, 0);
+    lv_obj_set_style_bg_color(vol_popup_, black, 0);
+    lv_obj_set_style_bg_opa(vol_popup_, LV_OPA_COVER, 0);
+    lv_obj_set_style_radius(vol_popup_, 10, 0);
+    lv_obj_set_style_border_width(vol_popup_, 0, 0);
+    lv_obj_set_style_pad_all(vol_popup_, 18, 0);
+    lv_obj_set_style_pad_row(vol_popup_, 12, 0);
+    lv_obj_set_flex_flow(vol_popup_, LV_FLEX_FLOW_COLUMN);
+    lv_obj_set_flex_align(vol_popup_, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(vol_popup_, LV_OBJ_FLAG_SCROLLABLE);
+    lv_obj_t* vol_title = lv_label_create(vol_popup_);
+    lv_label_set_text(vol_title, "音量");
+    lv_obj_set_style_text_color(vol_title, white, 0);
+    lv_obj_set_style_text_font(vol_title, big_font, 0);
+    lv_obj_t* vol_bars = lv_obj_create(vol_popup_);     // 5 格白块横排
+    lv_obj_set_size(vol_bars, LV_SIZE_CONTENT, LV_SIZE_CONTENT);
+    lv_obj_set_style_bg_opa(vol_bars, LV_OPA_TRANSP, 0);
+    lv_obj_set_style_border_width(vol_bars, 0, 0);
+    lv_obj_set_style_pad_all(vol_bars, 0, 0);
+    lv_obj_set_style_pad_column(vol_bars, 7, 0);
+    lv_obj_set_flex_flow(vol_bars, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(vol_bars, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(vol_bars, LV_OBJ_FLAG_SCROLLABLE);
+    for (int i = 0; i < 5; i++) {
+        lv_obj_t* seg = lv_obj_create(vol_bars);
+        lv_obj_set_size(seg, 34, 22);
+        lv_obj_set_style_radius(seg, 2, 0);
+        lv_obj_set_style_pad_all(seg, 0, 0);
+        lv_obj_set_style_border_width(seg, 2, 0);
+        lv_obj_set_style_border_color(seg, white, 0);
+        lv_obj_set_style_bg_color(seg, white, 0);
+        lv_obj_set_style_bg_opa(seg, LV_OPA_TRANSP, 0);   // 默认空（只白框），ShowVolume 点亮前 N 格
+        lv_obj_remove_flag(seg, LV_OBJ_FLAG_SCROLLABLE);
+        vol_segs_[i] = seg;
+    }
+    lv_obj_add_flag(vol_popup_, LV_OBJ_FLAG_HIDDEN);
+
+    /* ---- 暂停卡：KEY 单击暂停时常驻显示（自画两条竖杠当 ⏸，不依赖字体里有无该符号）---- */
+    pause_popup_ = lv_obj_create(screen);
+    lv_obj_set_scrollbar_mode(pause_popup_, LV_SCROLLBAR_MODE_OFF);
+    lv_obj_set_size(pause_popup_, LV_SIZE_CONTENT, LV_SIZE_CONTENT);
+    lv_obj_align(pause_popup_, LV_ALIGN_CENTER, 0, 0);
+    lv_obj_set_style_bg_color(pause_popup_, black, 0);
+    lv_obj_set_style_bg_opa(pause_popup_, LV_OPA_COVER, 0);
+    lv_obj_set_style_radius(pause_popup_, 10, 0);
+    lv_obj_set_style_border_width(pause_popup_, 0, 0);
+    lv_obj_set_style_pad_all(pause_popup_, 26, 0);    // 四边等距 → ⏸ 居中（不再放"暂停"字，免下半留黑）
+    lv_obj_set_flex_flow(pause_popup_, LV_FLEX_FLOW_COLUMN);
+    lv_obj_set_flex_align(pause_popup_, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(pause_popup_, LV_OBJ_FLAG_SCROLLABLE);
+    lv_obj_t* pause_bars = lv_obj_create(pause_popup_);   // 两条竖杠 = ⏸
+    lv_obj_set_size(pause_bars, LV_SIZE_CONTENT, LV_SIZE_CONTENT);
+    lv_obj_set_style_bg_opa(pause_bars, LV_OPA_TRANSP, 0);
+    lv_obj_set_style_border_width(pause_bars, 0, 0);
+    lv_obj_set_style_pad_all(pause_bars, 0, 0);
+    lv_obj_set_style_pad_column(pause_bars, 14, 0);
+    lv_obj_set_flex_flow(pause_bars, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(pause_bars, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(pause_bars, LV_OBJ_FLAG_SCROLLABLE);
+    for (int i = 0; i < 2; i++) {
+        lv_obj_t* bar = lv_obj_create(pause_bars);
+        lv_obj_set_size(bar, 18, 52);
+        lv_obj_set_style_radius(bar, 3, 0);
+        lv_obj_set_style_pad_all(bar, 0, 0);
+        lv_obj_set_style_border_width(bar, 0, 0);
+        lv_obj_set_style_bg_color(bar, white, 0);
+        lv_obj_set_style_bg_opa(bar, LV_OPA_COVER, 0);
+        lv_obj_remove_flag(bar, LV_OBJ_FLAG_SCROLLABLE);
+    }
+    lv_obj_add_flag(pause_popup_, LV_OBJ_FLAG_HIDDEN);
+
+    /* ---- 通知/提醒浮层：服务器推 alert(提醒到点)或系统通知时，居中黑卡显文字，到点自动隐。
+       盖在待命屏/对话之上 → 暖待命的时钟屏上也能看见提醒内容。---- */
+    notify_popup_ = lv_obj_create(screen);
+    lv_obj_set_scrollbar_mode(notify_popup_, LV_SCROLLBAR_MODE_OFF);
+    lv_obj_set_size(notify_popup_, LV_SIZE_CONTENT, LV_SIZE_CONTENT);
+    lv_obj_align(notify_popup_, LV_ALIGN_CENTER, 0, 0);
+    lv_obj_set_style_bg_color(notify_popup_, black, 0);
+    lv_obj_set_style_bg_opa(notify_popup_, LV_OPA_COVER, 0);
+    lv_obj_set_style_radius(notify_popup_, 10, 0);
+    lv_obj_set_style_border_width(notify_popup_, 0, 0);
+    lv_obj_set_style_pad_all(notify_popup_, 20, 0);
+    lv_obj_remove_flag(notify_popup_, LV_OBJ_FLAG_SCROLLABLE);
+    notify_msg_label_ = lv_label_create(notify_popup_);
+    lv_label_set_long_mode(notify_msg_label_, LV_LABEL_LONG_WRAP);
+    lv_obj_set_width(notify_msg_label_, 300);
+    lv_obj_set_style_text_align(notify_msg_label_, LV_TEXT_ALIGN_CENTER, 0);
+    lv_obj_set_style_text_color(notify_msg_label_, white, 0);
+    lv_obj_set_style_text_font(notify_msg_label_, small_font, 0);  // 占位，显示时换 common 全字集
+    lv_label_set_text(notify_msg_label_, "");
+    lv_obj_add_flag(notify_popup_, LV_OBJ_FLAG_HIDDEN);
+
+    /* 1s tick：切待命屏显隐 + 刷时间/温湿度/电池（活在 LVGL task，已持锁） */
+    idle_timer_ = lv_timer_create(IdleTimerCb, 1000, this);
+}
+
+// 字幕：当前句大字，旧句移到上方小字（替代 web 的"淡显"——1-bit 没灰阶，用字号层次）
+void CustomLcdDisplay::SetChatMessage(const char* role, const char* content) {
+    DisplayLockGuard lock(this);
+    if (chat_message_label_ == nullptr) {
+        return;
+    }
+    if (content == nullptr || content[0] == '\0') {
+        return;
+    }
+    // 首次（此刻 assets 已加载，SetupUI 时还没）把小字也换成 16px common 全字集——
+    // 否则"上句/本节课/状态"等任意文字落在 app 内嵌的 basic 子集上会缺字。
+    if (!conv_small_font_applied_) {
+        const lv_font_t* cf = CommonFont16();
+        if (cf != nullptr) {
+            lv_obj_t* small_labels[] = {prev_label_, topic_label_, who_label_,
+                                        status_label_, notification_label_};
+            for (auto lbl : small_labels) {
+                if (lbl != nullptr) {
+                    lv_obj_set_style_text_font(lbl, cf, 0);
+                }
+            }
+            conv_small_font_applied_ = true;
+        }
+    }
+    if (prev_label_ != nullptr) {
+        const char* cur = lv_label_get_text(chat_message_label_);
+        lv_label_set_text(prev_label_, (cur != nullptr) ? cur : "");
+    }
+    // 用当前主题字体——assets 在"刷新主题"时把 common 全字集 fonts.bin 设成了主题字体；
+    // SetupUI 时捕获的是 basic 内置精简子集（张老师任意文字会缺字，照片实证）。
+    auto theme = static_cast<LvglTheme*>(current_theme_);
+    if (theme != nullptr && theme->text_font() != nullptr) {
+        lv_obj_set_style_text_font(chat_message_label_, theme->text_font()->font(), 0);
+    }
+    lv_label_set_text(chat_message_label_, content);
+}
+
+// 1-bit 反射屏暂不显情绪表情（彩色/GIF 压成黑白会糟、且会压字幕）；后续可换一张 1-bit 简笔脸。
+void CustomLcdDisplay::SetEmotion(const char* emotion) {
+}
+
+// ============ 待命屏（idle 时整屏时钟） ============
+
+// 懒加载 16px common 全字集：assets 在 SetupUI 之后才 Apply，不能在 SetupUI 里取。
+const lv_font_t* CustomLcdDisplay::CommonFont16() {
+    if (small_common_font_ == nullptr) {
+        void* fptr = nullptr;
+        size_t fsize = 0;
+        if (Assets::GetInstance().GetAssetData("font_puhui_common_16_4.bin", fptr, fsize)) {
+            auto f = std::make_shared<LvglCBinFont>(fptr);
+            if (f->font() != nullptr) {
+                small_common_font_ = f;
+            }
+        }
+    }
+    return (small_common_font_ != nullptr) ? small_common_font_->font() : nullptr;
+}
+
+// SHTC3（0x70，与音频 codec 共用 I2C 总线）：唤醒 → 测量(T先/普通模式/不拉伸时钟) → 读6字节 → 睡眠。
+// idle 时每 ~30s 读一次；I2C 新驱动按总线串行、线程安全，且 idle 时 codec 无 I2C 流量，无争用。
+bool CustomLcdDisplay::ReadShtc3(float& temp_c, float& humi) {
+    if (i2c_bus_ == nullptr) {
+        return false;
+    }
+    if (shtc3_dev_ == nullptr) {
+        i2c_device_config_t dc = {};
+        dc.dev_addr_length = I2C_ADDR_BIT_LEN_7;
+        dc.device_address = 0x70;
+        dc.scl_speed_hz = 100000;
+        if (i2c_master_bus_add_device(i2c_bus_, &dc, &shtc3_dev_) != ESP_OK) {
+            shtc3_dev_ = nullptr;
+            return false;
+        }
+    }
+    // 不拉伸时钟(0x7866)+手动延时+分开读。两个实测坑：
+    // 1) 拉伸模式(0x7CA2,transmit_receive)会让传感器转换时拉住 SCL 超过 ESP32 容忍度
+    //    →"clear bus failed"→ESP_ERR_INVALID_STATE，100% 失败，别用。
+    // 2) 开机头 ~10s 音频 codec(ES8311/ES7210)在同一条 I2C 上初始化抢总线，这里 meas/read
+    //    报 INVALID_STATE；codec 初始化完(~10s)立刻恢复。靠上层 idle_env_ok_ 每秒重试自愈。
+    uint8_t wake[2] = {0x35, 0x17};                       // wakeup
+    if (i2c_master_transmit(shtc3_dev_, wake, 2, 50) != ESP_OK) {
+        return false;
+    }
+    vTaskDelay(pdMS_TO_TICKS(1));                          // 唤醒需 >240us
+    uint8_t meas[2] = {0x78, 0x66};                       // 测量：T 先 / 普通模式 / 不拉伸时钟
+    if (i2c_master_transmit(shtc3_dev_, meas, 2, 50) != ESP_OK) {
+        return false;
+    }
+    vTaskDelay(pdMS_TO_TICKS(15));                         // 普通模式测量最长 ~12.1ms
+    uint8_t rx[6] = {0};
+    esp_err_t r = i2c_master_receive(shtc3_dev_, rx, 6, 50);
+    uint8_t sleep_cmd[2] = {0xB0, 0x98};                  // sleep（省电）
+    i2c_master_transmit(shtc3_dev_, sleep_cmd, 2, 50);
+    if (r != ESP_OK) {
+        return false;
+    }
+    uint16_t raw_t = ((uint16_t)rx[0] << 8) | rx[1];
+    uint16_t raw_h = ((uint16_t)rx[3] << 8) | rx[4];
+    temp_c = -45.0f + 175.0f * (float)raw_t / 65535.0f;
+    humi   = 100.0f * (float)raw_h / 65535.0f;
+    return true;
+}
+
+// 3 根升序实心信号格（细弧字形在 1-bit 硬阈值屏会被吃掉、只剩中点；实心条/轴对齐边框锐利）。
+// 返回容器供父级 flex 摆放，out_bars 接收 3 个 bar（由 SetSignalBars 按档位点亮）。
+lv_obj_t* CustomLcdDisplay::MakeSignalBars(lv_obj_t* parent, lv_obj_t* out_bars[3]) {
+    lv_color_t black = lv_color_black();
+    lv_obj_t* box = lv_obj_create(parent);
+    lv_obj_set_size(box, LV_SIZE_CONTENT, 14);
+    lv_obj_set_style_bg_opa(box, LV_OPA_TRANSP, 0);
+    lv_obj_set_style_border_width(box, 0, 0);
+    lv_obj_set_style_pad_all(box, 0, 0);
+    lv_obj_set_style_pad_column(box, 2, 0);
+    lv_obj_set_flex_flow(box, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(box, LV_FLEX_ALIGN_START, LV_FLEX_ALIGN_END, LV_FLEX_ALIGN_END);  // 底对齐
+    lv_obj_remove_flag(box, LV_OBJ_FLAG_SCROLLABLE);
+    static const int BAR_H[3] = {6, 10, 14};
+    for (int i = 0; i < 3; i++) {
+        lv_obj_t* b = lv_obj_create(box);
+        lv_obj_set_size(b, 4, BAR_H[i]);
+        lv_obj_set_style_radius(b, 0, 0);
+        lv_obj_set_style_pad_all(b, 0, 0);
+        lv_obj_set_style_border_width(b, 1, 0);          // 空格=黑框白底；轴对齐边框在 1-bit 屏不糊
+        lv_obj_set_style_border_color(b, black, 0);
+        lv_obj_set_style_bg_color(b, black, 0);
+        lv_obj_set_style_bg_opa(b, LV_OPA_TRANSP, 0);    // 默认空，由 SetSignalBars 按档位点亮
+        lv_obj_remove_flag(b, LV_OBJ_FLAG_SCROLLABLE);
+        out_bars[i] = b;
+    }
+    return box;
+}
+
+// 实心电池：容器 = 外壳(白底黑框 + 黑色实心填充条) + 正极小帽。*out_fill 接收填充条(按电量改宽)。
+lv_obj_t* CustomLcdDisplay::MakeSolidBattery(lv_obj_t* parent, lv_obj_t** out_fill) {
+    lv_color_t black = lv_color_black();
+    lv_color_t white = lv_color_white();
+    lv_obj_t* box = lv_obj_create(parent);
+    lv_obj_set_size(box, LV_SIZE_CONTENT, LV_SIZE_CONTENT);
+    lv_obj_set_style_bg_opa(box, LV_OPA_TRANSP, 0);
+    lv_obj_set_style_border_width(box, 0, 0);
+    lv_obj_set_style_pad_all(box, 0, 0);
+    lv_obj_set_style_pad_column(box, 0, 0);
+    lv_obj_set_flex_flow(box, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(box, LV_FLEX_ALIGN_START, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(box, LV_OBJ_FLAG_SCROLLABLE);
+
+    lv_obj_t* bat = lv_obj_create(box);
+    lv_obj_set_size(bat, 28, 16);
+    lv_obj_set_style_radius(bat, 2, 0);
+    lv_obj_set_style_pad_all(bat, 2, 0);
+    lv_obj_set_style_border_width(bat, 2, 0);
+    lv_obj_set_style_border_color(bat, black, 0);
+    lv_obj_set_style_bg_color(bat, white, 0);
+    lv_obj_set_style_bg_opa(bat, LV_OPA_COVER, 0);
+    lv_obj_remove_flag(bat, LV_OBJ_FLAG_SCROLLABLE);
+
+    lv_obj_t* fill = lv_obj_create(bat);                  // 内容区 = 28-2*(2框+2pad)=20 宽, 8 高
+    lv_obj_set_style_radius(fill, 0, 0);
+    lv_obj_set_style_border_width(fill, 0, 0);
+    lv_obj_set_style_pad_all(fill, 0, 0);
+    lv_obj_set_style_bg_color(fill, black, 0);
+    lv_obj_set_style_bg_opa(fill, LV_OPA_COVER, 0);
+    lv_obj_set_height(fill, 8);
+    lv_obj_set_width(fill, 1);
+    lv_obj_align(fill, LV_ALIGN_LEFT_MID, 0, 0);
+
+    lv_obj_t* nub = lv_obj_create(box);                   // 正极小帽
+    lv_obj_set_size(nub, 3, 7);
+    lv_obj_set_style_radius(nub, 0, 0);
+    lv_obj_set_style_border_width(nub, 0, 0);
+    lv_obj_set_style_pad_all(nub, 0, 0);
+    lv_obj_set_style_bg_color(nub, black, 0);
+    lv_obj_set_style_bg_opa(nub, LV_OPA_COVER, 0);
+    lv_obj_set_style_margin_left(nub, 1, 0);
+
+    *out_fill = fill;
+    return box;
+}
+
+// 点亮前 level 格(实心)，其余只剩黑框
+void CustomLcdDisplay::SetSignalBars(lv_obj_t* const bars[3], int level) {
+    for (int i = 0; i < 3; i++) {
+        if (bars[i] != nullptr) {
+            lv_obj_set_style_bg_opa(bars[i], (i < level) ? LV_OPA_COVER : LV_OPA_TRANSP, 0);
+        }
+    }
+}
+
+// 按电量(0-100)设实心填充宽度（内宽 20）
+void CustomLcdDisplay::SetBatFill(lv_obj_t* fill, int pct) {
+    if (fill == nullptr) {
+        return;
+    }
+    int w = 20 * pct / 100;
+    if (w < 1) w = 1;
+    if (w > 20) w = 20;
+    lv_obj_set_width(fill, w);
+}
+
+// 整屏白底待命版面：顶 [Study Agent | wifi + 实心电池] / 中 大时间(24h) / 下 日期 / 再下 温湿度。
+// 建在对话三区之后→z 序盖住对话；初始隐藏，由 timer 在 idle 时显示。
+void CustomLcdDisplay::SetupIdleScreen(lv_obj_t* screen, const lv_font_t* big_font,
+                                       const lv_font_t* small_font) {
+    lv_color_t black = lv_color_black();
+    lv_color_t white = lv_color_white();
+
+    idle_screen_ = lv_obj_create(screen);
+    lv_obj_set_size(idle_screen_, LV_HOR_RES, LV_VER_RES);
+    lv_obj_set_style_radius(idle_screen_, 0, 0);
+    lv_obj_set_style_pad_all(idle_screen_, 0, 0);
+    lv_obj_set_style_border_width(idle_screen_, 0, 0);
+    lv_obj_set_style_bg_color(idle_screen_, white, 0);
+    lv_obj_set_style_bg_opa(idle_screen_, LV_OPA_COVER, 0);
+    lv_obj_remove_flag(idle_screen_, LV_OBJ_FLAG_SCROLLABLE);
+    lv_obj_align(idle_screen_, LV_ALIGN_TOP_LEFT, 0, 0);
+    lv_obj_add_flag(idle_screen_, LV_OBJ_FLAG_HIDDEN);   // 开机非 idle，先藏
+
+    /* 顶条：左 Study Agent | 右 wifi + 实心电池 */
+    lv_obj_t* top = lv_obj_create(idle_screen_);
+    lv_obj_set_width(top, LV_HOR_RES);
+    lv_obj_set_height(top, LV_SIZE_CONTENT);
+    lv_obj_set_style_radius(top, 0, 0);
+    lv_obj_set_style_bg_opa(top, LV_OPA_TRANSP, 0);
+    lv_obj_set_style_border_color(top, black, 0);
+    lv_obj_set_style_border_width(top, 1, 0);
+    lv_obj_set_style_border_side(top, LV_BORDER_SIDE_BOTTOM, 0);
+    lv_obj_set_style_pad_top(top, 5, 0);
+    lv_obj_set_style_pad_bottom(top, 5, 0);
+    lv_obj_set_style_pad_left(top, 8, 0);
+    lv_obj_set_style_pad_right(top, 8, 0);
+    lv_obj_set_flex_flow(top, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(top, LV_FLEX_ALIGN_SPACE_BETWEEN, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(top, LV_OBJ_FLAG_SCROLLABLE);
+    lv_obj_align(top, LV_ALIGN_TOP_MID, 0, 0);
+
+    idle_title_label_ = lv_label_create(top);
+    lv_label_set_text(idle_title_label_, "随手");
+    lv_obj_set_style_text_font(idle_title_label_, small_font, 0);
+
+    lv_obj_t* tr = lv_obj_create(top);   // 右组：wifi + 电池
+    lv_obj_set_size(tr, LV_SIZE_CONTENT, LV_SIZE_CONTENT);
+    lv_obj_set_style_bg_opa(tr, LV_OPA_TRANSP, 0);
+    lv_obj_set_style_border_width(tr, 0, 0);
+    lv_obj_set_style_pad_all(tr, 0, 0);
+    lv_obj_set_style_pad_column(tr, 0, 0);
+    lv_obj_set_flex_flow(tr, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(tr, LV_FLEX_ALIGN_END, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_remove_flag(tr, LV_OBJ_FLAG_SCROLLABLE);
+
+    /* 信号格 + 实心电池：与对话顶条共用 builder（实心条/轴对齐边框在 1-bit 屏锐利，细弧字形会被吃掉） */
+    lv_obj_t* idle_wifi_box = MakeSignalBars(tr, idle_wifi_bars_);
+    lv_obj_set_style_margin_right(idle_wifi_box, 8, 0);
+    MakeSolidBattery(tr, &idle_bat_fill_);
+
+    /* 中央：大时间 HH:MM（24h） */
+    idle_time_label_ = lv_label_create(idle_screen_);
+    lv_label_set_text(idle_time_label_, "--:--");
+    lv_obj_set_style_text_font(idle_time_label_, big_font, 0);
+    lv_obj_set_style_text_color(idle_time_label_, black, 0);
+    lv_obj_align(idle_time_label_, LV_ALIGN_CENTER, 0, -18);
+
+    /* 日期：6月24日 周二 */
+    idle_date_label_ = lv_label_create(idle_screen_);
+    lv_label_set_text(idle_date_label_, "");
+    lv_obj_set_style_text_font(idle_date_label_, small_font, 0);
+    lv_obj_set_style_text_color(idle_date_label_, black, 0);
+    lv_obj_align(idle_date_label_, LV_ALIGN_CENTER, 0, 18);
+
+    /* 温湿度：26.5°C  湿度55% */
+    idle_env_label_ = lv_label_create(idle_screen_);
+    lv_label_set_text(idle_env_label_, "");
+    lv_obj_set_style_text_font(idle_env_label_, small_font, 0);
+    lv_obj_set_style_text_color(idle_env_label_, black, 0);
+    lv_obj_align(idle_env_label_, LV_ALIGN_CENTER, 0, 44);
+}
+
+// 1s tick：切待命屏显隐；idle 时刷时间(每分变)/日期/温湿度(30s)/电池(30s)/wifi。
+// 反射屏每次刷新都整屏推 SPI，故所有 set_text 都先比对缓存，值没变就不动→不触发 flush。
+void CustomLcdDisplay::UpdateIdleScreen() {
+    if (idle_screen_ == nullptr) {
+        return;
+    }
+    bool idle = (Application::GetInstance().GetDeviceState() == kDeviceStateIdle);
+    if (idle != idle_visible_) {
+        idle_visible_ = idle;
+        if (idle) {
+            lv_obj_remove_flag(idle_screen_, LV_OBJ_FLAG_HIDDEN);
+            idle_env_tick_ = 0;   // 进 idle 立刻读一次温湿度/电池
+        } else {
+            lv_obj_add_flag(idle_screen_, LV_OBJ_FLAG_HIDDEN);
+        }
+    }
+    // ---- wifi + 电池：idle 屏和对话顶条两套都刷（隐藏的那套不绘制、开销忽略；保证切换时不残旧）----
+    // wifi 信号格(每秒)：GetNetworkStateIcon 字形 → 0-3 格（满格/一般/弱→3/2/1）
+    const char* wifi = Board::GetInstance().GetNetworkStateIcon();
+    int wlvl;
+    if (wifi == nullptr)                                wlvl = 0;
+    else if (strcmp(wifi, FONT_AWESOME_WIFI) == 0)      wlvl = 3;
+    else if (strcmp(wifi, FONT_AWESOME_WIFI_FAIR) == 0) wlvl = 2;
+    else if (strcmp(wifi, FONT_AWESOME_WIFI_WEAK) == 0) wlvl = 1;
+    else                                                wlvl = 0;   // slash/未连
+    if (wlvl != last_wifi_lvl_) {
+        last_wifi_lvl_ = wlvl;
+        SetSignalBars(idle_wifi_bars_, wlvl);
+        SetSignalBars(conv_wifi_bars_, wlvl);
+    }
+    // 电池(每 ~30s，ADC 有噪)：两套填充按电量
+    if (idle_env_tick_ == 0) {
+        int lvl = 0; bool chg = false, dis = false;
+        if (Board::GetInstance().GetBatteryLevel(lvl, chg, dis) && lvl != last_bat_pct_) {
+            last_bat_pct_ = lvl;
+            SetBatFill(idle_bat_fill_, lvl);
+            SetBatFill(conv_bat_fill_, lvl);
+        }
+    }
+
+    // ---- 以下仅 idle 屏：时钟/日期/温湿度（对话态跳过）----
+    if (idle) {
+        // 中文标签换 common 全字集（basic 子集缺「周/湿/度/°」）；assets 此刻已就绪。
+        if (!idle_font_applied_) {
+            const lv_font_t* cf = CommonFont16();
+            if (cf != nullptr) {
+                lv_obj_t* labels[] = {idle_title_label_, idle_date_label_, idle_env_label_};
+                for (auto l : labels) {
+                    if (l != nullptr) {
+                        lv_obj_set_style_text_font(l, cf, 0);
+                    }
+                }
+                idle_font_applied_ = true;
+            }
+        }
+
+        time_t now = time(nullptr);
+        struct tm tmv;
+        localtime_r(&now, &tmv);
+        bool time_ok = (tmv.tm_year >= 2025 - 1900);
+
+        char tbuf[8];
+        if (time_ok) {
+            snprintf(tbuf, sizeof(tbuf), "%02d:%02d", tmv.tm_hour, tmv.tm_min);
+        } else {
+            snprintf(tbuf, sizeof(tbuf), "--:--");
+        }
+        if (strcmp(tbuf, idle_last_time_) != 0) {
+            strncpy(idle_last_time_, tbuf, sizeof(idle_last_time_) - 1);
+            lv_label_set_text(idle_time_label_, tbuf);
+        }
+
+        static const char* const WD[] = {"周日", "周一", "周二", "周三", "周四", "周五", "周六"};
+        char dbuf[40];
+        if (time_ok) {
+            snprintf(dbuf, sizeof(dbuf), "%d月%d日 %s", tmv.tm_mon + 1, tmv.tm_mday, WD[tmv.tm_wday]);
+        } else {
+            dbuf[0] = '\0';
+        }
+        if (strcmp(dbuf, idle_last_date_) != 0) {
+            strncpy(idle_last_date_, dbuf, sizeof(idle_last_date_) - 1);
+            lv_label_set_text(idle_date_label_, dbuf);
+        }
+
+        // 温湿度：30s 刷一次；首次读到前每秒重试（开机 codec 抢 I2C，前几次会失败，别干等 30s）
+        if (idle_env_tick_ == 0 || !idle_env_ok_) {
+            float t = 0, h = 0;
+            if (ReadShtc3(t, h)) {
+                idle_env_ok_ = true;
+                char ebuf[40];
+                snprintf(ebuf, sizeof(ebuf), "%.1f°C  湿度%.0f%%", (double)t, (double)h);
+                if (strcmp(ebuf, idle_last_env_) != 0) {
+                    strncpy(idle_last_env_, ebuf, sizeof(idle_last_env_) - 1);
+                    lv_label_set_text(idle_env_label_, ebuf);
+                }
+            }
+        }
+    }
+    idle_env_tick_ = (idle_env_tick_ + 1) % 30;
+}
+
+void CustomLcdDisplay::IdleTimerCb(lv_timer_t* t) {
+    auto* self = static_cast<CustomLcdDisplay*>(lv_timer_get_user_data(t));
+    if (self != nullptr) {
+        self->UpdateIdleScreen();
+    }
+}
+
+// 双击 KEY 调音量：居中黑卡点亮前 N 格（20→1格 … 100→5格），~2.5s 后自动隐。
+// 从按键任务调用 → 必须持显示锁；VolTimerCb 跑在 LVGL task（已持锁）不再加锁。
+void CustomLcdDisplay::ShowVolume(int pct) {
+    DisplayLockGuard lock(this);
+    if (vol_popup_ == nullptr) {
+        return;
+    }
+    int lvl = (pct + 19) / 20;            // 20→1, 40→2, 60→3, 80→4, 100→5
+    if (lvl < 1) lvl = 1;
+    if (lvl > 5) lvl = 5;
+    for (int i = 0; i < 5; i++) {
+        lv_obj_set_style_bg_opa(vol_segs_[i], i < lvl ? LV_OPA_COVER : LV_OPA_TRANSP, 0);
+    }
+    lv_obj_remove_flag(vol_popup_, LV_OBJ_FLAG_HIDDEN);
+    lv_obj_move_foreground(vol_popup_);   // 压在待命屏/对话之上
+    if (vol_timer_ == nullptr) {
+        vol_timer_ = lv_timer_create(VolTimerCb, 2500, this);
+    } else {
+        lv_timer_reset(vol_timer_);       // 2.5s 内再调一次 → 续命
+        lv_timer_resume(vol_timer_);
+    }
+}
+
+void CustomLcdDisplay::VolTimerCb(lv_timer_t* t) {
+    auto* self = static_cast<CustomLcdDisplay*>(lv_timer_get_user_data(t));
+    if (self != nullptr && self->vol_popup_ != nullptr) {
+        lv_obj_add_flag(self->vol_popup_, LV_OBJ_FLAG_HIDDEN);
+    }
+    lv_timer_pause(t);   // 一次性：隐藏后停，下次 ShowVolume 再 reset+resume
+}
+
+// KEY 单击暂停/恢复：居中「⏸ 暂停」卡显/隐，常驻到再按（设备本地态，与桥 paused 逐次同步）。
+void CustomLcdDisplay::ShowPause(bool on) {
+    DisplayLockGuard lock(this);
+    if (pause_popup_ == nullptr) {
+        return;
+    }
+    if (on) {
+        lv_obj_remove_flag(pause_popup_, LV_OBJ_FLAG_HIDDEN);
+        lv_obj_move_foreground(pause_popup_);   // 压在待命屏/对话/音量条之上
+    } else {
+        lv_obj_add_flag(pause_popup_, LV_OBJ_FLAG_HIDDEN);
+    }
+}
+
+// 服务器推 alert(提醒到点)/系统通知：居中黑卡显文字，duration_ms 后自动隐。
+// 压在待命屏之上 → 暖待命时钟屏上也能看见提醒内容。同名再调=换文+续命。
+void CustomLcdDisplay::ShowNotification(const char* notification, int duration_ms) {
+    DisplayLockGuard lock(this);
+    if (notify_popup_ == nullptr || notify_msg_label_ == nullptr) {
+        return;
+    }
+    const lv_font_t* cf = CommonFont16();   // 中文全字集(assets 就绪后)，替掉缺字的 basic
+    if (cf != nullptr) {
+        lv_obj_set_style_text_font(notify_msg_label_, cf, 0);
+    }
+    lv_label_set_text(notify_msg_label_, notification ? notification : "");
+    lv_obj_remove_flag(notify_popup_, LV_OBJ_FLAG_HIDDEN);
+    lv_obj_move_foreground(notify_popup_);   // 压在待命屏/对话之上
+    int dur = duration_ms > 0 ? duration_ms : 3000;
+    if (notify_timer_ == nullptr) {
+        notify_timer_ = lv_timer_create(NotifyTimerCb, dur, this);
+    } else {
+        lv_timer_set_period(notify_timer_, dur);
+        lv_timer_reset(notify_timer_);
+        lv_timer_resume(notify_timer_);
+    }
+}
+
+void CustomLcdDisplay::NotifyTimerCb(lv_timer_t* t) {
+    auto* self = static_cast<CustomLcdDisplay*>(lv_timer_get_user_data(t));
+    if (self != nullptr && self->notify_popup_ != nullptr) {
+        lv_obj_add_flag(self->notify_popup_, LV_OBJ_FLAG_HIDDEN);
+    }
+    lv_timer_pause(t);   // 一次性：隐藏后停，下次 ShowNotification 再 reset+resume
+}
diff --git a/main/boards/waveshare/esp32-s3-rlcd-4.2/custom_lcd_display.h b/main/boards/waveshare/esp32-s3-rlcd-4.2/custom_lcd_display.h
index 7ca91cc..e5f2bb5 100644
--- a/main/boards/waveshare/esp32-s3-rlcd-4.2/custom_lcd_display.h
+++ b/main/boards/waveshare/esp32-s3-rlcd-4.2/custom_lcd_display.h
@@ -2,8 +2,12 @@
 #define __CUSTOM_LCD_DISPLAY_H__
 
 #include <driver/gpio.h>
+#include <driver/i2c_master.h>
+#include <memory>
 #include "lcd_display.h"
 
+class LvglFont;   // 前向声明：小字用的 16px common 全字集（assets 加载后懒加载）
+
 enum ColorSelection {
     ColorBlack = 0,    
     ColorWhite = 0xff
@@ -32,6 +36,58 @@ private:
     int                 height_;
     uint8_t            *DispBuffer = NULL;
     int                 DisplayLen;
+    lv_obj_t           *who_label_  = nullptr;   // 顶条右：用户/学科
+    lv_obj_t           *topic_label_ = nullptr;  // 底部：本节课主要内容
+    lv_obj_t           *prev_label_  = nullptr;  // 字幕·上一句（小）
+    std::shared_ptr<LvglFont> small_common_font_ = nullptr;  // 16px common 全字集：assets 就绪后给小字用，替掉缺字的 basic 子集
+    bool                conv_small_font_applied_ = false;     // 对话小字是否已换成 common
+
+    // ---- 待命屏：idle 时盖在对话版面上的整屏时钟（日期/时间/温湿度/Study Agent/实心电池）----
+    i2c_master_bus_handle_t i2c_bus_      = nullptr;   // 与音频 codec 共用，SHTC3 挂同一总线
+    i2c_master_dev_handle_t shtc3_dev_    = nullptr;   // 温湿度传感器（0x70），首次读时懒建
+    lv_timer_t         *idle_timer_       = nullptr;   // 1s tick：切显隐 + 刷时间/温湿度/电池
+    lv_obj_t           *idle_screen_      = nullptr;   // 整屏白底容器（不透明，盖住对话）
+    lv_obj_t           *idle_title_label_ = nullptr;   // "Study Agent"
+    lv_obj_t           *idle_time_label_  = nullptr;   // 大字 HH:MM（24h）
+    lv_obj_t           *idle_date_label_  = nullptr;   // 6月24日 周二
+    lv_obj_t           *idle_env_label_   = nullptr;   // 26.5°C  湿度55%
+    lv_obj_t           *idle_wifi_bars_[3] = {nullptr, nullptr, nullptr};  // 右上信号格：细弧字形在 1-bit 屏会被阈值吃掉，改实心竖条
+    lv_obj_t           *idle_bat_fill_    = nullptr;   // 实心电池的填充条（按电量改宽度）
+    lv_obj_t           *conv_wifi_bars_[3] = {nullptr, nullptr, nullptr};  // 对话顶条信号格（同 idle，换掉细弧字形）
+    lv_obj_t           *conv_bat_fill_    = nullptr;   // 对话顶条实心电池填充条
+    bool                idle_visible_     = false;
+    bool                idle_font_applied_= false;     // idle 中文标签是否已换 common
+    uint32_t            idle_env_tick_    = 0;         // 温湿度 30s 读一次的计数
+    bool                idle_env_ok_      = false;     // 是否读到过温湿度（没读到前每秒重试，开机首读偶发失败别干等 30s）
+    char                idle_last_time_[8]  = {0};
+    char                idle_last_date_[40] = {0};
+    char                idle_last_env_[40]  = {0};
+    int                 last_wifi_lvl_      = -2;       // 信号格数缓存（idle+对话共用，-2=未设）
+    int                 last_bat_pct_       = -1;       // 电量缓存（idle+对话共用）
+    const lv_font_t*    CommonFont16();                // 懒加载 16px common 全字集
+    lv_obj_t*           MakeSignalBars(lv_obj_t* parent, lv_obj_t* out_bars[3]);  // 3 根实心信号格(返回容器)
+    lv_obj_t*           MakeSolidBattery(lv_obj_t* parent, lv_obj_t** out_fill);  // 实心电池(返回容器, *out_fill=填充条)
+    void                SetSignalBars(lv_obj_t* const bars[3], int level);        // 点亮前 level 格
+    void                SetBatFill(lv_obj_t* fill, int pct);                      // 按电量设填充宽
+    bool                ReadShtc3(float& temp_c, float& humi);
+    void                SetupIdleScreen(lv_obj_t* screen, const lv_font_t* big_font,
+                                        const lv_font_t* small_font);
+    void                UpdateIdleScreen();
+    static void         IdleTimerCb(lv_timer_t* t);
+
+    // ---- 音量条：双击 KEY 调音量时，居中黑卡 + 5 格白块，常驻 ~2.5s 自动隐 ----
+    lv_obj_t           *vol_popup_       = nullptr;
+    lv_obj_t           *vol_segs_[5]     = {nullptr, nullptr, nullptr, nullptr, nullptr};
+    lv_timer_t         *vol_timer_       = nullptr;
+    static void         VolTimerCb(lv_timer_t* t);
+    lv_obj_t           *pause_popup_     = nullptr;   // KEY 单击暂停时常驻的「⏸ 暂停」居中卡（自画双竖条，不依赖字体里有该符号）
+
+    // ---- 通知/提醒浮层：服务器推 alert(提醒到点)或系统通知时，居中黑卡显文字，到点自动隐 ----
+    lv_obj_t           *notify_popup_     = nullptr;
+    lv_obj_t           *notify_msg_label_ = nullptr;
+    lv_timer_t         *notify_timer_     = nullptr;
+    static void         NotifyTimerCb(lv_timer_t* t);
+
 	uint16_t (*PixelIndexLUT)[300];
 	uint8_t  (*PixelBitLUT  )[300];
 	void InitPortraitLUT();
@@ -46,12 +102,22 @@ private:
 public:
     CustomLcdDisplay(esp_lcd_panel_io_handle_t panel_io, esp_lcd_panel_handle_t panel,
                   int width, int height, int offset_x, int offset_y,
-                  bool mirror_x, bool mirror_y, bool swap_xy,spi_display_config_t spiconfig,spi_host_device_t spi_host = SPI3_HOST);
+                  bool mirror_x, bool mirror_y, bool swap_xy,spi_display_config_t spiconfig,spi_host_device_t spi_host = SPI3_HOST,
+                  i2c_master_bus_handle_t i2c_bus = nullptr);
     ~CustomLcdDisplay();
     void RLCD_Init();
     void RLCD_ColorClear(uint8_t color);
     void RLCD_Display();
 	void RLCD_SetPixel(uint16_t x, uint16_t y, uint8_t color);
+
+    // 张老师盒子·黑白屏重排：顶条(wifi/状态/用户·学科) + 中央字幕(当前句大/上句小) + 底部本节课
+    void SetupUI() override;
+    void SetChatMessage(const char* role, const char* content) override;
+    void SetEmotion(const char* emotion) override;
+    void ShowVolume(int pct);   // 双击 KEY 调音量：居中 5 格音量条，~2.5s 自动隐
+    void ShowPause(bool on);    // 单击 KEY 暂停/恢复：居中「⏸ 暂停」卡显/隐（常驻到再按）
+    // 服务器推 alert(提醒到点)/系统通知：居中黑卡显文字，duration_ms 后自动隐（盖在待命屏之上）
+    void ShowNotification(const char* notification, int duration_ms = 3000) override;
 };
 
 #endif
\ No newline at end of file
diff --git a/main/boards/waveshare/esp32-s3-rlcd-4.2/waveshare-s3-rlcd-4.2.cc b/main/boards/waveshare/esp32-s3-rlcd-4.2/waveshare-s3-rlcd-4.2.cc
index faa89ca..06102c2 100644
--- a/main/boards/waveshare/esp32-s3-rlcd-4.2/waveshare-s3-rlcd-4.2.cc
+++ b/main/boards/waveshare/esp32-s3-rlcd-4.2/waveshare-s3-rlcd-4.2.cc
@@ -19,6 +19,8 @@ class CustomBoard : public WifiBoard {
 private:
     i2c_master_bus_handle_t i2c_bus_;
     Button boot_button_;
+    Button key_button_;
+    bool key_paused_ = false;          // KEY 单击的本地暂停态（驱动屏上 ⏸ 显/隐）
     CustomLcdDisplay *display_;
     adc_oneshot_unit_handle_t adc1_handle;
     adc_cali_handle_t cali_handle;
@@ -55,6 +57,20 @@ private:
             }
         });
 #endif
+
+        // 侧键 KEY：单击=暂停/恢复（发 self.pause.toggle，桥按住/继续推音频）；双击=音量+一档循环
+        key_button_.OnClick([this]() {
+            key_paused_ = !key_paused_;                        // 本地记暂停态 → 屏上 ⏸ 显/隐
+            if (display_) display_->ShowPause(key_paused_);
+            Application::GetInstance().SendMcpMessage("{\"jsonrpc\":\"2.0\",\"method\":\"self.pause.toggle\"}");
+        });
+        key_button_.OnDoubleClick([this]() {
+            auto codec = GetAudioCodec();
+            int v = (codec->output_volume() / 20) * 20 + 20;   // 下一档（20 的倍数）
+            if (v > 100) v = 20;                                // 满了回到 20
+            codec->SetOutputVolume(v);
+            if (display_) display_->ShowVolume(v);             // 屏上 5 格音量条（~2.5s 自动隐）
+        });
     }
 
     void InitializeTools() {
@@ -73,7 +89,8 @@ private:
         spi_config.dc = RLCD_DC_PIN;
         spi_config.cs = RLCD_CS_PIN;
         spi_config.rst = RLCD_RST_PIN;
-        display_ = new CustomLcdDisplay(NULL, NULL, RLCD_WIDTH,RLCD_HEIGHT,DISPLAY_OFFSET_X,DISPLAY_OFFSET_Y,DISPLAY_MIRROR_X,DISPLAY_MIRROR_Y,DISPLAY_SWAP_XY,spi_config);
+        // 末两参：spi_host（默认 SPI3）+ i2c_bus（给待命屏的 SHTC3 用，与音频 codec 同总线）
+        display_ = new CustomLcdDisplay(NULL, NULL, RLCD_WIDTH,RLCD_HEIGHT,DISPLAY_OFFSET_X,DISPLAY_OFFSET_Y,DISPLAY_MIRROR_X,DISPLAY_MIRROR_Y,DISPLAY_SWAP_XY,spi_config,SPI3_HOST,i2c_bus_);
     }
 
     uint16_t BatterygetVoltage(void) {
@@ -130,7 +147,7 @@ private:
     }
 
 public:
-    CustomBoard() : boot_button_(BOOT_BUTTON_GPIO) {    
+    CustomBoard() : boot_button_(BOOT_BUTTON_GPIO), key_button_(KEY_BUTTON_GPIO) {
         InitializeI2c();  
         InitializeButtons();     
         InitializeTools();
diff --git a/sdkconfig.defaults b/sdkconfig.defaults
index ae6b193..35447fd 100644
--- a/sdkconfig.defaults
+++ b/sdkconfig.defaults
@@ -76,3 +76,19 @@ CONFIG_LV_USE_WIN=n
 CONFIG_LV_BUILD_EXAMPLES=n
 CONFIG_LV_BUILD_DEMOS=n
 
+# --- 张老师盒子：非交互选定这块板（来自 board config.json） ---
+CONFIG_BOARD_TYPE_WAVESHARE_ESP32_S3_RLCD_4_2=y
+CONFIG_USE_WECHAT_MESSAGE_STYLE=n
+CONFIG_USE_DEVICE_AEC=y
+
+# --- 张老师盒子：唤醒词改「你好老师」---
+# 内置 wakenet 词里没有"你好老师"，改用 MultiNet 自定义词（拼音、空格分、无声调）。
+# 自定义词识别率比训练好的低一档，阈值小=更灵敏（默认20）；不灵就调 CUSTOM_WAKE_WORD_THRESHOLD。
+CONFIG_USE_AFE_WAKE_WORD=n
+CONFIG_USE_CUSTOM_WAKE_WORD=y
+CONFIG_CUSTOM_WAKE_WORD="ni hao lao shi"
+CONFIG_CUSTOM_WAKE_WORD_DISPLAY="你好老师"
+# 自定义词走 MultiNet，必须选一个中文 MN 模型，否则 build 报"no multinet models selected"。
+# esp32s3 通用中文识别用 mn7_cn。
+CONFIG_SR_MN_CN_MULTINET7_QUANT=y
+
